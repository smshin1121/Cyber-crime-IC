# web — AI Context Map

> **Stack:** flask | none | unknown | python

> 7 routes | 0 models | 0 components | 2 lib files | 0 env vars | 0 middleware
> **Token savings:** this file is ~300 tokens. Without it, AI exploration would cost ~4,400 tokens. **Saves ~4,100 tokens per conversation.**

---

# Routes

- `GET` `/` [auth, db, cache]
- `GET` `/wiki/<slug>` params(slug) [auth, db, cache]
- `GET` `/wiki/<category>/<slug>` params(category, slug) [auth, db, cache]
- `GET` `/wiki/_missing/<slug>` params(slug) [auth, db, cache]
- `GET` `/category/<category>` params(category) [auth, db, cache]
- `GET` `/search` [auth, db, cache]
- `GET` `/stats` [auth, db, cache]

---

# Libraries

- `app.py`
  - function resolve_wiki_path: (slug)
  - function get_category_for_file: (filepath)
  - function parse_page: (filepath)
  - function convert_wikilinks: (text)
  - function convert_callouts: (html)
  - function open_external_links_in_new_tab: (html) -> str
  - _...16 more_
- `build_static.py` — function build: () -> None

---

_Generated by [codesight](https://github.com/Houseofmvps/codesight) — see your codebase clearly_